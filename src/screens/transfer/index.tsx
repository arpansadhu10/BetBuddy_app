import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { BottomDoc, Screen } from "../../components";

export default function Transfer() {
  return (
    <Screen>
      <Text>Transfer</Text>
      <BottomDoc active="Transfer" />
    </Screen>
  );
}

const styles = StyleSheet.create({});
